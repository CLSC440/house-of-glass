{
  "name": "gallery-website",
  "version": "1.0.0",
  "description": "Static Gallery with Serverless API",
  "scripts": {
    "dev": "npx vercel dev",
    "start": "npx vercel dev",
    "preview:static": "python -m http.server 8000"
  },
  "dependencies": {
    "firebase-admin": "^11.5.0",
    "pg": "^8.19.0"
  },
  "engines": {
    "node": "22.x"
  }
}
